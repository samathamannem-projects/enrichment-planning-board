import React from "react";
import ReactDOM from "react-dom/client";
import EnrichmentBoard from "./EnrichmentBoard.jsx";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <EnrichmentBoard />
  </React.StrictMode>
);
